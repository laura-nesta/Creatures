using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlgoGenetique : MonoBehaviour
{
    //string str = "Fuck fucking fucked fucker fucking fuckups fuck fucking fucked fucking fuckup fucking fucker's fucking fuckup.";
   
    //public float chance_to_mutate = 0.1;
    //public float grated_retain_percent = 0.2;
    //pourcentage d'individus "haut gradés" qui seront retenu pour la génération suivante

    //public float chance_retain_nongrated = 0.05;
    // pourcentatge de chance qu'un individu faiblement gradé soit qd meme retenupour la gen suiv

    //public int pop_count = 100;
    //nb d'individus dans la population

    //public int gene_count_max = 100000;
    //nb max de generation => stoppe le script si une solution n'est pas trouvée à temps

    //public int graded_ind_retain_count = pop_count * grated_retain_percent;

    //public int length_str = str.Length;
    //public int middle_length_str = str.Length / 2;

    //public int max_score = length_str;

   
    // fonction qui renvoie un caractère aléatoire

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
