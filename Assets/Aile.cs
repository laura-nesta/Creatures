using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Aile : MonoBehaviour
{
/*
    
    Vector3 m_StartPos, m_StartForce;
    Vector3 m_NewForce;
    public Rigidbody m_Aile;

    float masse = 0.5f;

    public float angle;
    float amplitude = 1.0f;
    Vector3 portance;
    void Start() {

        m_Aile = GetComponent<Rigidbody>();

        m_NewForce = new Vector3(0.0f, 8.0f, 0.0f);

        portance = new Vector3(0.0f, 0.1f, 0.0f);

        m_StartPos = transform.position;
        m_StartForce = m_Aile.transform.position;

        m_Aile.isKinematic = true;
    }

    void Update() 
    {
        transform.Rotate(-Mathf.Sin(angle) * amplitude, 0, 0);
        angle +=0.1f;
    }
    public float angleMax = 1.5f;
    public bool bat = false;


    public void tt() {
        bat = true;
        transform.Rotate(Mathf.Sin(angle) * amplitude, 0, 0);
        angle +=0.1f;
    }

    public void tt2() {
        bat = true;
        transform.Rotate(-Mathf.Sin(angle) * amplitude, 0, 0);
        angle +=0.1f;
    }

    public void tt3() {
        bat = false;
        transform.Rotate(0, 0, 0);
        angle = 0;
        
    }
    */

}
